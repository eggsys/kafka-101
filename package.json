{
  "name": "kafka-01",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "start:producer" : "ts-node-dev --respawn --transpile-only src/producer.ts",
    "start:consumer" : "ts-node-dev --respawn --transpile-only src/consumer.ts",
    "start:producer:01" : "ts-node-dev --respawn --transpile-only src/begin/producer-01.ts",
    "start:consumer:01" : "ts-node-dev --respawn --transpile-only src/begin/consumer-01.ts"
  },
  "license": "MIT",
  "dependencies": {
    "kafkajs": "^1.16.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.3"
  }
}
